# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/codecollab

# Session Secret (generate a strong random string)
SESSION_SECRET=your-super-secret-session-key-here

# Google OAuth Configuration
CLIENT_ID=your-google-client-id.apps.googleusercontent.com
CLIENT_GOOGLE_SECRET=your-google-client-secret

# Optional: Custom Google Callback URL (auto-detected if not set)
# For localhost: http://localhost:3002/auth/google/callback
# For Vercel: https://your-domain.vercel.app/auth/google/callback
# GOOGLE_CALLBACK_URL=https://your-app-name.vercel.app/auth/google/callback

# Environment (auto-detected in most cases)
# NODE_ENV=development  # Use for local development
# NODE_ENV=production   # Use for production
# VERCEL_ENV=production # Auto-set by Vercel
