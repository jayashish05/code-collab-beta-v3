# CodeCollab Environment Variables
# Rename this file to .env and fill in your values

# Application
PORT=3002
NODE_ENV=development

# Session Secret (IMPORTANT: Replace this with a strong random string in production)
# To generate a secure random string, run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your_strong_random_secret_here

# Google OAuth (Required for Google authentication)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
CALLBACK_URL=http://localhost:3002/auth/google/callback

# When deploying to Vercel, set these in the Vercel dashboard instead
# For production deployment, use:
# CALLBACK_URL=https://code-collab-beta-v1.vercel.app/auth/google/callback
